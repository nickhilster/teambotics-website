---
import '../styles/tokens.css';
import '../styles/blog.css';
import '../styles/article-cta.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  markdownAlternate?: string;
}

const { title, description = 'Insights on industrial automation and robotics from the Teambotics team.', ogImage, markdownAlternate } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const brandAssetBase = 'https://www.teambotics.app/brand';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" href={`${brandAssetBase}/teambotics-logo-32.png`} />
    <link rel="canonical" href={canonicalURL} />
    {markdownAlternate && (
      <link rel="alternate" type="text/markdown" title={`${title} (agent-readable markdown)`} href={markdownAlternate} />
    )}
    <link rel="alternate" type="text/plain" href="/llms.txt" title="llms.txt" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:type" content="website" />
    {ogImage && <meta property="og:image" content={ogImage} />}
    <meta name="twitter:card" content="summary_large_image" />
    <title>{title}</title>
  </head>
  <body>
    <header class="blog-header">
      <div class="blog-header__inner">
        <a href="https://blog.teambotics.app" class="blog-header__brand">
          <picture class="blog-header__logo">
            <source
              media="(max-width: 719px)"
              srcSet={`${brandAssetBase}/teambotics-logo-24.png 1x, ${brandAssetBase}/teambotics-logo-48.png 2x`}
            />
            <img
              alt=""
              decoding="async"
              height="28"
              src={`${brandAssetBase}/teambotics-logo-28.png`}
              srcSet={`${brandAssetBase}/teambotics-logo-28.png 1x, ${brandAssetBase}/teambotics-logo-56.png 2x`}
              width="28"
            />
          </picture>
          <span class="blog-header__site-name">Teambotics Blog</span>
        </a>
        <nav class="blog-header__nav">
          <a href="/values" class="blog-header__nav-link">
            Values
          </a>
          <a href="https://www.teambotics.app" class="blog-header__nav-link" target="_blank" rel="noopener noreferrer">
            Main site ↗
          </a>
        </nav>
      </div>
    </header>

    <main class="blog-main">
      <slot />
    </main>

    <footer class="blog-footer">
      <div class="blog-footer__inner">
        <p>© {new Date().getFullYear()} Teambotics. All rights reserved.</p>
        <a href="https://www.teambotics.app" class="blog-footer__link" target="_blank" rel="noopener noreferrer">teambotics.app</a>
      </div>
    </footer>

    <script>
      const renderMermaid = async () => {
        const nodes = document.querySelectorAll('.mermaid');
        if (nodes.length === 0) return;

        const isDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        const mod = await import('mermaid');
        const mermaid = mod.default;
        mermaid.initialize({ startOnLoad: false, theme: isDark ? 'dark' : 'neutral' });
        await mermaid.run({ nodes: Array.from(nodes) });
      };

      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', renderMermaid, { once: true });
      } else {
        renderMermaid();
      }
    </script>
  </body>
</html>
